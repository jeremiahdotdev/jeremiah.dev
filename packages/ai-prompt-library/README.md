# AI Prompt Library

Shared prompt templates for AI-assisted content generation in the monorepo.

## Included prompts

- `buildProfileJsonAuthoringPrompt`: Generates a prompt for authoring profile JSON files under `packages/profile-data`.

## Goal

This package captures the repo's approved schema, writing rules, and validation checklist so prompt text can be reused consistently across apps or scripts.
